# Deploy-only subset for the Streamlit dashboard (Render).
# The dashboard imports src/config.py and src/calendar_events.py, so it needs
# requests + pytz but none of the ML stack.
streamlit==1.57.0
pandas==2.3.3
numpy==2.4.0
boto3==1.43.9
botocore==1.43.9
python-dateutil==2.9.0.post0
pytz==2025.2
plotly==6.7.0
requests==2.32.5
